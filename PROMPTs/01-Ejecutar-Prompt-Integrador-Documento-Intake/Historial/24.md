# Validá en el navegador

http://192.168.1.109:8080/ · ⚠️ incógnito / borrá cookies.

1. Creá el admin → login → Alta de equipos → dá de alta tu SAI real.
2. Dejá correr el poller unos minutos (sondea cada 5 s) para que se acumulen muestras.
3. Andá a Históricos y gráficas → elegí período (p. ej. Última hora) y variables (tensión de entrada/salida) → Graficar. Vas a ver la evolución real de la tensión de tu SAI en la gráfica, la lista de eventos del período y el conteo de microcortes.
Si probás un período vacío (p. ej. antes del alta), sale PERIODO_SIN_DATOS (no dibuja serie vacía).


## Abrí el PR
https://github.com/UTN-FRP-TUP-Aplicada-2025/SAI.Service.Core/pull/new/feature/etapa-3d-historicos

```
Título:


Etapa 3·D — Históricos y gráficas (US-11)
Descripción:


## Resumen
Cierra la Etapa 3: el panel de históricos y gráficas.

- **`ServicioHistoricos`** (solo lectura): elige la fuente por **retención** (muestras crudas ≤30 días, agregados horarios más atrás), computa los agregados **on-demand** con `CalculadorAgregado` cuando no hay persistidos, adjunta **cobertura + advertencia** a la serie agregada (I-20, RN-10) y responde `PERIODO_SIN_DATOS` sin dibujar serie vacía.
- **Conteo de microcortes desde los eventos**, nunca del promedio agregado (CL-16).
- **Repositorio**: consultas por período (muestras, agregados, eventos).
- **Web**: `HistoricosYGraficas.razor` con `MudChart` (selector de período/variables, banda de cobertura, tabla de eventos y conteo de microcortes).

La purga por retención (choca con el interceptor append-only) queda diferida. **2 pruebas nuevas · suite 121/121 + 3 omitidas.**
```

Validá la gráfica y, con tu OK, mergeás/borrás la rama y avisás.

Con esto queda cerrada la Etapa 3 (monitoreo en vivo, salud e históricos). La próxima es la Etapa 4 — Verificación y ciclo de vida, la etapa crítica: el apagado ordenado real, el bloqueo por verificación y la ventana de mantenimiento (donde por fin se habilita la escritura por NUT contra tu SAI). Cuando quieras, arrancamos.

---
ya hice el merge del PR, y el delete de la rama, ahora observo lo que te dejo segun la captura , ahora el historial lo esta haciendo, evalua esa captura y decime
---

Evalué la captura: es el comportamiento correcto y esperado, nada roto. 👍

Lo que pasa: para validar los históricos (Incremento D) desplegué con el adaptador NUT (tu SAI real). Y el adaptador NUT, a propósito, no ejecuta el autotest de batería — lanzarlo de verdad requiere un comando de escritura (INSTCMD) con credenciales de administrador en NUT, que está diferido a la Etapa 4. Por eso, al tocar «Lanzar prueba», el panel responde con el aviso honesto:

⚠️ El autotest por NUT requiere credenciales de escritura (diferido). Probá con el adaptador simulado.

Y el historial queda vacío porque, correctamente, no se registró ninguna prueba (el test no corrió). Todo funciona como se diseñó:

Con el adaptador simulado (como validaste en el Incremento C), el autotest sí corre, emite la curva de descarga y registra el veredicto en el historial.
Con el adaptador NUT (el de ahora), la lectura/monitoreo/históricos andan contra tu SAI real, pero la escritura (autotest, apagado) está bloqueada hasta la Etapa 4.
Dicho de otro modo: la captura confirma que la guarda de "solo lectura" del NUT funciona y que el mensaje al usuario es claro. No hay que arreglar nada.

Para seguir validando los históricos (Incremento D) dejá el deploy como está (NUT). El poller está juntando muestras cada 5 s; en unos minutos, en Históricos y gráficas vas a ver la curva real. Si en cambio querés volver a probar el autotest de batería, decímelo y redesplego con el simulado (pero perdés los datos reales del SAI para el gráfico).

¿Seguimos? Cuando valides los históricos y mergees/borres… ya está mergeado D, así que con tu OK arrancamos la Etapa 4 (donde por fin se habilita la escritura por NUT: apagado ordenado, bloqueo por verificación y ventana de mantenimiento).