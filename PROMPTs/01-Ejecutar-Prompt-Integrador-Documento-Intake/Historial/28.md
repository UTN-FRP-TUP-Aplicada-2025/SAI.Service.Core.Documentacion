Confirmado: en el código mergeado el menú sí tiene "Apagado ordenado" (MainLayout.razor:107) y RegistroDeIntervenciones.razor ya no tiene el placeholder de Sprint 0. La instancia que estás viendo es un build anterior a B y C — hay que recompilar y reiniciar la app desde el main actualizado.

Pasos:

Frená la app que está corriendo (Ctrl+C en la terminal donde la lanzaste).
Actualizá tu checkout local a main:

cd /home/fernando/workspaces/workspace-dev/DEV/SAI.Service.Core
git checkout main && git pull
Recompilá y relanzá (con el mismo comando/entorno que usás normalmente). Si es dotnet run:

dotnet run --project src/SAI.Service.Core/SAI.Service.Core.Web -c Release
El arranque aplicará solas las migraciones EsquemaAcciones y EsquemaIntervenciones sobre tu base.
Después de eso vas a ver en el menú "Apagado ordenado" (B) y, en Registro de intervenciones, el formulario real de recambio con el cuadre de costos en vivo y la ficha.

Un detalle: la página de intervenciones necesita un equipo en servicio. Si tu base actual no tiene un alta hecha, va a mostrar "No hay un equipo en servicio" con un link a "Dá de alta un equipo" — hacé el alta primero y después el recambio.

¿Querés que siga con el Incremento D (reparación/sustitución del SAI, US-20) — el último de la Etapa 4 — mientras validás, o preferís confirmar primero que ya ves B y C en el navegador?