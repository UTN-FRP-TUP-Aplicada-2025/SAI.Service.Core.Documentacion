# Etapa 1 en verde — resumen

Arranqué con 5 tests de integración en rojo y los llevé a 27/27 verdes diagnosticando 4 causas de fondo (no parches):

Síntoma	Causa raíz	Fix
500 AmbiguousMatchException en /alta-inicial y /acceso	Páginas placeholder del Sprint 0 duplicaban las rutas @page	Borré AltaInicialAdministrador.razor y AccesoLogin.razor
Rol/admin "ya existe", fallos flaky que cambiaban entre corridas	La cadena de conexión se leía eager → todos los tests compartían sai.db	Lectura diferida desde el ServiceProvider en DependencyInjection.cs
Token ROPC válido pero /api/v1/ping → 401	La validación JWT leía la clave eager (appsettings) y el firmado vía IOptions (override) → claves distintas	Validación diferida vía AddOptions<JwtBearerOptions>().Configure<IOptions<OpcionesJwt>> en Program.cs
GET / sin admin redirigía a /acceso, no a /alta-inicial	Faltaba la capa de ruteo de la guarda de primer arranque (estaban superficie + acción)	Middleware de primer arranque en Program.cs
Build 0/0, dotnet format limpio.

App levantada para que valides visualmente
http://192.168.1.109:8080/ (contenedor sai-web-e1, Producción, HTTP 8080). Reemplazó a la de Sprint 0.

Flujo sugerido en el navegador:

Entrás a / → te desvía a /alta-inicial (primer arranque, no hay admin).
Creás el administrador (usuario + contraseña).
Iniciás sesión → panel con el tema de la maqueta (verde #0F6E56, sidebar oscuro, Inter).
Barra superior: Cambiar contraseña y Cerrar sesión.
Y la API ROPC (después de crear el admin), desde tu terminal:


TOKEN=$(curl -s -X POST http://192.168.1.109:8080/api/v1/token \
  -d 'grant_type=password&username=admin&password=TU_CONTRASEÑA' \
  | grep -oP '"access_token":"\K[^"]+')
curl -s http://192.168.1.109:8080/api/v1/ping -H "Authorization: Bearer $TOKEN"   # → 200 + JSON
Cierre documental ya preparado (sin commitear aún)
ADR-28 — autenticación de la API con Bearer JWT vía ROPC (esquema dual con la cookie; complementa ADR-16). Índice de decisiones → v1.4 (28 ADR).
CHANGELOG.md — entrada de Etapa 1.
.publish-e1/ excluido en .gitignore.
Estoy en la rama feature/etapa-1-persistencia-acceso. Validá visualmente el flujo y, cuando me des el OK, hago el commit de Etapa 1 y te preparo el PR. Si algo del diseño no refleja la maqueta, decímelo y lo ajusto antes de commitear.


---

me loguee, me solicito usuario y contraseña con confirmacion de contraseña. 
me hizo un redirect a  http://192.168.1.109:8080/alta-inicial

```
Esta página no funciona
Si el problema persiste, ponte en contacto con el propietario del sitio web.
HTTP ERROR 400

```
revisa a ver cual es el problema


---